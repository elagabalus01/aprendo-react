let activo = true
let mensaje = activo ? 'activo' :'inactivo'
console.log(mensaje)

activo = false
mensaje = activo ? 'activo' :'inactivo'
console.log(mensaje)

let mensajeDefault = true && 'mensaje por defecto'


console.log(mensajeDefault)
